#!/usr/bin/env sh
# Pre-push: lint + unit tests + 70% coverage in one Gradle invocation.
set -eu

ROOT=$(git rev-parse --show-toplevel)
cd "$ROOT"

echo "==> Lint + unit tests + coverage"
make check-app

echo "Pre-push checks passed."
