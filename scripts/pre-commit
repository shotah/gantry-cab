#!/usr/bin/env sh
# Pre-commit: script tests + JVM unit tests. Lint + coverage are pre-push / CI.
set -eu

ROOT=$(git rev-parse --show-toplevel)
cd "$ROOT"

echo "==> Script tests"
make test-scripts

echo "==> JVM unit tests"
make test-app

echo "Pre-commit checks passed."
