org.gradle.jvmargs=-Xmx2048m -XX:+UseParallelGC -XX:MaxMetaspaceSize=512m -Dfile.encoding=UTF-8
org.gradle.caching=true
org.gradle.configuration-cache=true
org.gradle.configuration-cache.parallel=true
org.gradle.parallel=true
kotlin.daemon.jvmargs=-Xmx2g
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official
# AGP 9 built-in Kotlin (do not apply org.jetbrains.kotlin.android).
# JDK 21 is what we run locally and in CI.
# Raise heaps in ~/.gradle/gradle.properties on a large local box — CI runners are ~7 GB.
